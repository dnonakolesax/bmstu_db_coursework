on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: docker run -d -it dnonakolesax/bmstu-course-db:latest bash 
    - uses: actions/checkout@v2
    
  test:
    runs-on: ubuntu-latest
    steps:
    - run: docker run -d -it dnonakolesax/bmstu-course-db:latest bash --name bmstu-course-db
    - run: docker exec -it bmstu-course-db:latest -d python test/auth_test.py
    - uses: actions/checkout@v2